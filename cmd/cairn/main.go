// Command cairn assembles files and writes them into a directory.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "cairn: not implemented yet — see docs/plan.md")
	os.Exit(1)
}
